{
  "name": "verdaccio-coauth",
  "version": "0.0.1",
  "description": "Verdaccio authentication plugin that delegates authentication to another HTTP URL",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "MIT",
  "keywords": [
    "verdaccio"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/nicolindemann/sinopia-delegated-auth.git"
  },
  "dependencies": {
    "request": "^2.72.0"
  }
}
